#include "Util.h"
#include <cstring>
#include <string>
#include <stdexcept>
#define __FILENAME__                                                           \
  (strrchr(__FILE__, '/') ? strrchr(__FILE__, '/') + 1 : __FILE__)

// TODO
double calcCotan(const Vector3d &first, const Vector3d &second) {
  throw std::runtime_error("TODO: Implement " + std::string(__FUNCTION__) +
                           "(...) in " + std::string(__FILENAME__));
}
